# Guardrails
- Never expose secrets or hardcode API keys.
- Keep admin routes behind auth + role checks.
- Do not bypass validation and error handling.
- Preserve loading, empty, and error states in all features.
- Keep mobile responsiveness on every route.
- Update architecture and README when changing core structure.
